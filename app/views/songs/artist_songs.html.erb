<div id='title'>
  <h1>All Songs for <%= @artist.name %> </h1>
</div>

<% @songs.each do |song| %>
  <div id='song-<%= song.id %>' class='song'>
    <h2><%= song.title %></h2>
    <p>Play Count: <%= song.play_count %></p>
  </div>
<% end %>
