<h1 id='title'>
  All Songs
  <% unless @artist.nil? %>
    for
    <%= @artist.name %>
  <% end %>
</h1>

<% @songs.each do |song| %>
  <div id='song-<%= song.id %>' class='song'>
    <h2><%= song.title %></h2>
    <p>Play Count: <%= song.play_count %></p>
  </div>
<% end %>
